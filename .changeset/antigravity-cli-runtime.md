---
"@runfusion/fusion": minor
---

summary: Add Antigravity CLI (`agy`) runtime provider so subscription quota can power Fusion lanes.
category: feature
dev: New bundled plugin `fusion-plugin-antigravity-runtime` (provider `antigravity-cli`, runtime `antigravity`); settings `useAntigravityCli` / `antigravityCliBinaryPath` / `antigravityCliPermissionMode`. Does not restore deprecated Pi OAuth antigravity providers.
