---
"@runfusion/fusion": patch
---

summary: Reclaim built task worktrees containing only Fusion's regenerable build cache.
category: fix
dev: Defensive cleanup probes top-level `.fusion` and accepts only its `cache` child.
